Hound
=====


Usage
-----

```
  Usage: hound [options] <path>

  Options:

    -h, --help           output usage information
    -V, --version        output the version number
    -t, --type <type>    report type to generate
    -r, --report <path>  directory to write report to
```
